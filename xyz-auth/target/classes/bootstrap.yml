server:
  port: 10088
spring:
  application:
    name: xyz-auth
# 注册中心配置       
eureka:
  client:
    service-url:
      defaultZone: http://crss-registry:9010/eureka/
  instance:
    prefer-ip-address: true